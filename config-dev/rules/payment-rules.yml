rules:

# Rule 1: Validate Required Metadata
- name: "PAYMENT - Validate Required Metadata"
  description: "Ensure eventMetadata is not null or empty"
  priority: 2
  condition: "eventMetadata == null || eventMetadata.trim().isEmpty()"
  actions:
    - "envelope.setIgnore(true)"
    - "System.out.println('Rule: Ignoring event with missing metadata: ' + envelope.getEventId())"


# Rule 2: Ignore Test Events
- name: "PAYMENT - Ignore Test Events"
  description: "Ignore events from test environments"
  priority: 1
  condition: "eventSource != null && eventSource.toLowerCase().contains('test')"
  actions:
    - "envelope.setIgnore(true)"
    - "System.out.println('Rule: Ignoring TEST event: ' + envelope.getEventId())"


# Rule 3: Validate Event Payload
- name: "PAYMENT - Validate Payload"
  description: "Ensure event payload is not empty"
  priority: 2
  condition: "eventPayload == null || eventPayload.trim().isEmpty() || eventPayload.equals('{}') || eventPayload.equals('null')"
  actions:
    - "envelope.setIgnore(true)"
    - "System.out.println('Rule: Ignoring event with empty payload: ' + envelope.getEventId())"


# Rule 4: Default Brand if Non-Numeric
- name: "PAYMENT - Default ACQ_ICA"
  description: "Set brand to 0 if non-numeric"
  priority: 3
  condition: "envelope.getEventPayload().contains('brand') && !envelope.getEventPayload().matches('.*\"brand\":\"[0-9]+\".*')"
  actions:
    - "envelope.setEventPayload(envelope.getEventPayload().replaceAll('\"brand\":\"[^\"]*\"','\"brand\":\"0\"'))"
    - "System.out.println('Rule: Brand corrected to 0 for event: ' + envelope.getEventId())"


# Rule 5: Ignore if Transaction ID Missing
- name: "PAYMENT - Missing Transaction ID"
  description: "Ignore events where paymentTransactionId is missing"
  priority: 1
  condition: "!envelope.getEventPayload().contains('paymentTransactionId')"
  actions:
    - "envelope.setIgnore(true)"
    - "System.out.println('Rule: Missing paymentTransactionId for event: ' + envelope.getEventId())"


# Rule 6: Validate Event Source
- name: "PAYMENT - Validate Event Source"
  description: "Ignore events from invalid sources"
  priority: 1
  condition: "eventSource == null || (!eventSource.toUpperCase().contains('PYT') && !eventSource.toUpperCase().contains('PMT') && !eventSource.toUpperCase().contains('PAY'))"
  actions:
    - "envelope.setIgnore(true)"
    - "System.out.println('Rule: Invalid event source ' + eventSource + ' for event: ' + envelope.getEventId())"


# Rule 7: Validate Regulatory Region
- name: "PAYMENT - Validate Regulatory Region"
  description: "Allow only STL or ATL"
  priority: 1
  condition: "regulatoryRegion == null || (!regulatoryRegion.equalsIgnoreCase('STL') && !regulatoryRegion.equalsIgnoreCase('ATL'))"
  actions:
    - "envelope.setIgnore(true)"
    - "System.out.println('Rule: Invalid regulatory region ' + regulatoryRegion + ' for event: ' + envelope.getEventId())"


# Rule 8: Validate Payment Amount
- name: "PAYMENT - Validate Payment Amount"
  description: "Ignore events where paymentAmount is missing"
  priority: 2
  condition: "!envelope.getEventPayload().contains('paymentAmount')"
  actions:
    - "envelope.setIgnore(true)"
    - "System.out.println('Rule: Invalid paymentAmount for event: ' + envelope.getEventId())"


# Rule 9: Validate Payment Currency
- name: "PAYMENT - Validate Payment Currency"
  description: "Ignore events where currency is missing"
  priority: 2
  condition: "!envelope.getEventPayload().contains('currency')"
  actions:
    - "envelope.setIgnore(true)"
    - "System.out.println('Rule: Missing currency for event: ' + envelope.getEventId())"


# Rule 10: Validate Payment Method
- name: "PAYMENT - Validate Payment Method"
  description: "Ignore events where paymentMethod is missing"
  priority: 2
  condition: "!envelope.getEventPayload().contains('paymentMethod')"
  actions:
    - "envelope.setIgnore(true)"
    - "System.out.println('Rule: Missing paymentMethod for event: ' + envelope.getEventId())"


# Rule 11: Validate Debtor Account URI
- name: "PAYMENT - Validate Debtor Account URI"
  description: "Ignore events where debtorAccountUri is missing"
  priority: 2
  condition: "!envelope.getEventPayload().contains('debtorAccountUri')"
  actions:
    - "envelope.setIgnore(true)"
    - "System.out.println('Rule: Missing debtorAccountUri for event: ' + envelope.getEventId())"


# Rule 12: Default Response Code
- name: "PAYMENT - Default Response Code"
  description: "Set responseCode to UNKNOWN if missing"
  priority: 3
  condition: "!envelope.getEventPayload().contains('responseCode')"
  actions:
    - "envelope.setEventPayload(envelope.getEventPayload().replace('}',' ,\"responseCode\":\"UNKNOWN\"}'))"
    - "System.out.println('Rule: responseCode defaulted for event: ' + envelope.getEventId())"


# Rule 13: Default SW_SER_NUM
- name: "PAYMENT - Default SW_SER_NUM"
  description: "Ensure switchSerialNumber is numeric"
  priority: 3
  condition: "envelope.getEventPayload().contains('switchSerialNumber') && !envelope.getEventPayload().matches('.*\"switchSerialNumber\":\"[0-9]+\".*')"
  actions:
    - "envelope.setEventPayload(envelope.getEventPayload().replaceAll('\"switchSerialNumber\":\"[^\"]*\"','\"switchSerialNumber\":\"0000\"'))"
    - "System.out.println('Rule: switchSerialNumber defaulted to 0000 for event: ' + envelope.getEventId())"
